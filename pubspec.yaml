name: levir
description: Common part used across shrine front-end projects.
version: 0.0.1
homepage: https://github.com/shrinex/levir

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.1
  dio: ^5.1.2
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.4.0
  mockito: ^5.4.0